# Environment Configuration Example
# Copy this file to .env and .env.production and fill in the actual values

# Node.js Environment
NODE_ENV=development
PORT=3001

# Frontend API URL (used by Vite/React client-side)
# Must have VITE_ prefix for Vite to expose to browser
# Local: http://localhost:3001
# Production: https://your-domain.com
VITE_API_URL=http://localhost:3001

# Server's own URL (used by server to generate URLs pointing to itself)
# Should match VITE_API_URL in most cases
# Local: http://localhost:3001
# Production: https://your-domain.com
SERVER_URL=http://localhost:3001