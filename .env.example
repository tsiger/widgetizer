# Environment Configuration Example
# Copy this file to .env and .env.production and fill in the actual values

# Node.js Environment
NODE_ENV=development
PORT=3001

# Frontend API URL (used by Vite/React client-side)
# Must have VITE_ prefix for Vite to expose to browser
# Local: http://localhost:3001
# Production: https://your-domain.com
VITE_API_URL=http://localhost:3001

# Server's own URL (used by server to generate URLs pointing to itself)
# Should match VITE_API_URL in most cases
# Local: http://localhost:3001
# Production: https://your-domain.com
SERVER_URL=http://localhost:3001

# ── Preview Isolation (optional, for multi-user / SaaS deployments) ──
# When enabled, the preview iframe loads from a separate origin to prevent
# preview scripts from accessing the builder's cookies and storage.
# All values below are required when PREVIEW_ISOLATION=true.

# Enable cross-origin preview isolation
# PREVIEW_ISOLATION=true

# The origin of the builder app (used for postMessage origin verification)
# BUILDER_ORIGIN=https://builder.example.com

# Comma-separated CORS allowlist (both builder and preview origins)
# ALLOWED_ORIGINS=https://builder.example.com,https://preview.example.com

# Client-side equivalents (must have VITE_ prefix for Vite)
# VITE_PREVIEW_ISOLATION=true
# VITE_PREVIEW_ORIGIN=https://preview.example.com