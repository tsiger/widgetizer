<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />

		{% seo %}

		{% fonts_preconnect %}
		{% fonts_stylesheet %}

		{% asset "base.css" %}
		{% asset "header_scripts.js" %}

		{% theme_settings %}

		<style>
			*,
			*::before,
			*::after {
				box-sizing: border-box;
			}

			img,
			video {
				max-width: 100%;
				height: auto;
			}

			body {
				margin: 0;
				padding: 30px 0;
				font-family: var(--typography-body_font-family);
				font-weight: var(--typography-body_font-weight);
				color: var(--colors-text);
				background-color: var(--colors-background);
			}

			.container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 1rem;
			}

			h1,
			h2,
			h3,
			h4,
			h5,
			h6 {
				margin: 0;
				padding: 0;
				font-family: var(--typography-heading_font-family);
				font-weight: var(--typography-heading_font-weight);
			}
		</style>
	</head>
	<body class="{{ body_class }}">
		<!-- THEME SETTINGS DEBUG - Remove this section when done testing -->
		<div style="background: #f0f0f0; padding: 20px; margin-bottom: 20px; border: 2px solid #ccc; font-family: monospace; font-size: 12px; line-height: 1.6;">
			<h3 style="margin: 0 0 15px 0; color: #333;">Theme Settings Test Values:</h3>

			<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 15px;">

				<!-- Cards Group -->
				<div>
					<strong>Cards:</strong><br>
					Card Background: {{ theme.cards.card_background }}<br>
					Card Text: {{ theme.cards.card_text }}<br>
				</div>

				<!-- Colors Group -->
				<div>
					<strong>Colors:</strong><br>
					Background: {{ theme.colors.background }}<br>
					Text: {{ theme.colors.text }}<br>
					Accent: {{ theme.colors.accent }}<br>
				</div>

				<!-- Typography Group -->
				<div>
					<strong>Typography:</strong><br>
					Heading Font: {{ theme.typography.heading_font | json }}<br>
					Body Font: {{ theme.typography.body_font | json }}<br>
					Base Font Size: {{ theme.typography.base_font_size }}px<br>
				</div>

				<!-- Layout Group -->
				<div>
					<strong>Layout:</strong><br>
					Show Header: {{ theme.layout.show_header }}<br>
					Site Width: {{ theme.layout.site_width }}<br>
					Container Padding: {{ theme.layout.container_padding }}px<br>
				</div>

				<!-- Content Group -->
				<div>
					<strong>Content:</strong><br>
					Site Title: {{ theme.content.site_title }}<br>
					Site Description: {{ theme.content.site_description }}<br>
					Logo Image: {{ theme.content.logo_image | json }}<br>
					Hero Video: {{ theme.content.hero_video | json }}<br>
					Main Navigation: {{ theme.content.main_navigation }}<br>
					CTA Link: {{ theme.content.cta_link | json }}<br>
				</div>

				<!-- Features Group -->
				<div>
					<strong>Features:</strong><br>
					Enable Animations: {{ theme.features.enable_animations }}<br>
					Theme Style: {{ theme.features.theme_style }}<br>
					Border Radius: {{ theme.features.border_radius }}px<br>
				</div>
			</div>

			<!-- CSS Variables Test -->
			<div style="margin-top: 20px; padding: 15px; background: white; border: 1px solid #ddd;">
				<strong>CSS Variables Test:</strong><br>
				<div style="background: var(--cards-card_background); color: #333; padding: 10px; margin: 5px 0;">
					Card test. Background from 'card_background' setting. Text is hardcoded.
				</div>
				<div style="font-family: var(--typography-heading_font-family); font-weight: var(--typography-heading_font-weight); font-size: calc(1px * var(--typography-base_font_size) * 1.5); margin: 5px 0;">
					Heading font test. Size is 1.5x base.
				</div>
				<div style="font-family: var(--typography-body_font-family); font-weight: var(--typography-body_font-weight); font-size: calc(1px * var(--typography-base_font_size)); margin: 5px 0;">
					Body font test. Size is base.
				</div>
				<div style="padding: 10px; margin: 5px 0; border: 2px solid var(--colors-accent); border-radius: calc(1px * var(--features-border_radius));">
					Container with accent border and radius from 'border_radius' setting.
				</div>
			</div>
		</div>
		<!-- END THEME SETTINGS DEBUG -->

		<div class="container">{{ content }}</div>

		{% asset "footer_scripts.js" %}
	</body>
</html>
