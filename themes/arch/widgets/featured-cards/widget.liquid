<section
  id="{{ widget.id }}"
  class="widget widget-featured-cards"
  data-widget-id="{{ widget.id }}"
  data-widget-type="featured-cards"
>
  <style>
    #{{ widget.id }} {
      & .featured-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--space-lg);
      }

      & .featured-card {
        display: flex;
        flex-direction: column;
        background: var(--surface-secondary);
        border-radius: var(--radius-md);
        overflow: hidden;
        border: var(--border-width-thin) solid var(--border-color);
      }

      & .featured-image-container {
        position: relative;
      }

      & .featured-image {
        width: 100%;
        height: 200px;
        object-fit: cover;
      }

      & .featured-tag {
        position: absolute;
        top: var(--space-sm);
        left: var(--space-sm);
        background: var(--color-primary);
        color: white;
        padding: var(--space-xs) var(--space-sm);
        border-radius: var(--radius-sm);
        font-size: var(--font-size-xs);
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      & .featured-content {
        padding: var(--space-lg);
        display: flex;
        flex-direction: column;
        gap: var(--space-sm);
      }

      & .featured-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: var(--space-md);
      }

      & .featured-name {
        font-size: var(--font-size-xl);
        margin: 0;
      }

      & .featured-pricing {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        flex-shrink: 0;
      }

      & .featured-price {
        font-size: var(--font-size-xl);
        color: var(--color-primary);
      }

      & .featured-original-price {
        font-size: var(--font-size-base);
        color: var(--text-muted);
        text-decoration: line-through;
      }

      & .featured-description {
        color: var(--text-secondary);
        font-size: var(--font-size-sm);
        margin: 0;
        line-height: 1.6;
      }

      & .featured-availability {
        display: inline-flex;
        align-items: center;
        gap: var(--space-xs);
        font-size: var(--font-size-sm);
        color: var(--text-muted);
        margin-block-start: var(--space-xs);
      }

      & .featured-availability::before {
        content: "🕐";
      }

      /* List style variant */
      &.style-list .featured-grid {
        gap: 0;
      }

      &.style-list .featured-card {
        flex-direction: row;
        border-radius: 0;
        border-left: none;
        border-right: none;
        border-top: none;
        background: transparent;
      }

      &.style-list .featured-card:first-child {
        border-top: var(--border-width-thin) solid var(--border-color);
      }

      &.style-list .featured-image-container {
        flex-shrink: 0;
        width: 120px;
      }

      &.style-list .featured-image {
        height: 120px;
        border-radius: var(--radius-sm);
      }

      &.style-list .featured-content {
        flex: 1;
        padding: var(--space-md);
      }

      @media (min-width: 750px) {
        & .featured-grid {
          grid-template-columns: repeat(2, 1fr);
        }

        &.style-list .featured-grid {
          grid-template-columns: 1fr;
        }
      }

      @media (min-width: 990px) {
        & .featured-grid {
          grid-template-columns: repeat(3, 1fr);
        }
      }
    }
  </style>

  <div class="widget-container">
    {% if widget.settings.title != blank or widget.settings.description != blank or widget.settings.eyebrow != blank %}
      <div class="widget-header">
        {% if widget.settings.eyebrow != blank %}
          <span class="widget-eyebrow" data-setting="eyebrow">{{ widget.settings.eyebrow }}</span>
        {% endif %}
        {% if widget.settings.title != blank %}
          <h2 class="widget-headline" data-setting="title">{{ widget.settings.title }}</h2>
        {% endif %}
        {% if widget.settings.description != blank %}
          <p class="widget-description" data-setting="description">{{ widget.settings.description }}</p>
        {% endif %}
      </div>
    {% endif %}

    <div class="widget-content {% if widget.settings.style == 'list' %}style-list{% endif %}">
      <div class="featured-grid">
        {% for blockId in widget.blocksOrder %}
          {% assign block = widget.blocks[blockId] %}
          <div class="featured-card" data-block-id="{{ blockId }}">
            {% if block.settings.image != blank %}
              <div class="featured-image-container">
                {{ block.settings.image | image: 'medium', 'featured-image' }}
                {% if block.settings.tag != blank %}
                  <span class="featured-tag" data-setting="tag">{{ block.settings.tag }}</span>
                {% endif %}
              </div>
            {% endif %}

            <div class="featured-content">
              <div class="featured-header">
                {% if block.settings.name != blank %}
                  <h3 class="featured-name" data-setting="name">{{ block.settings.name }}</h3>
                {% endif %}
                <div class="featured-pricing">
                  {% if block.settings.original_price != blank %}
                    <span class="featured-original-price" data-setting="original_price">
                      {{- block.settings.original_price -}}
                    </span>
                  {% endif %}
                  {% if block.settings.price != blank %}
                    <span class="featured-price" data-setting="price">{{ block.settings.price }}</span>
                  {% endif %}
                </div>
              </div>

              {% if block.settings.description != blank %}
                <p class="featured-description" data-setting="description">{{ block.settings.description }}</p>
              {% endif %}

              {% if block.settings.availability != blank %}
                <span class="featured-availability" data-setting="availability">{{ block.settings.availability }}</span>
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>
