<section
  id="{{ widget.id }}"
  class="widget widget-type-logo-cloud widget-{{ widget.id }} color-scheme-{{ widget.settings.color_scheme }}"
  {% if widget.settings.color_scheme == 'dark' %}
    style="--widget-bg-color: var(--bg-primary);"
  {% endif %}
  data-widget-id="{{ widget.id }}"
  data-widget-type="logo-cloud"
  {% if widget.index != null %}data-widget-index="{{ widget.index }}"{% endif %}
>
  <style>
    .widget-{{ widget.id }} {
      & .logo-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: var(--space-lg);
        list-style: none;
        padding: 0;
        margin: 0;
        align-items: center;
        justify-items: center;
      }

      & .logo-item {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: var(--space-md);
        background-color: var(--bg-primary);
        border: var(--border-width-thin) solid var(--border-color);
      }

      & .logo-image {
        max-width: 18rem;
        height: auto;
        max-height: 8rem;
        width: auto;
        filter: grayscale(100%);
        opacity: 0.6;
        transition: opacity 0.3s, filter 0.3s;
        display: block;

        &:hover {
          opacity: 1;
          filter: grayscale(0%);
        }
      }

      & .logo-link {
        display: block;
      }

      @media (min-width: 750px) {
        & .logo-grid {
          grid-template-columns: repeat(3, 1fr);
        }

        & .logo-image {
          max-width: 20rem;
          max-height: 9rem;
        }
      }

      @media (min-width: 990px) {
        & .logo-grid {
          grid-template-columns: repeat(6, 1fr);
        }

        & .logo-image {
          max-width: 22rem;
          max-height: 10rem;
        }
      }
    }
  </style>

  <div class="widget-container {% if widget.settings.color_scheme == 'dark' %}widget-container-padded{% endif %}">
    {% if widget.settings.title != blank or widget.settings.description != blank or widget.settings.eyebrow != blank %}
      <div class="widget-header">
        {% if widget.settings.eyebrow != blank %}
          <span class="widget-eyebrow" data-setting="eyebrow">{{ widget.settings.eyebrow }}</span>
        {% endif %}
        {% if widget.settings.title != blank %}
          <h2 class="widget-headline" data-setting="title">{{ widget.settings.title }}</h2>
        {% endif %}
        {% if widget.settings.description != blank %}
          <p class="widget-description" data-setting="description">{{ widget.settings.description }}</p>
        {% endif %}
      </div>
    {% endif %}

    <div class="widget-content">
      <ul class="logo-grid">
        {% for blockId in widget.blocksOrder %}
          {% assign block = widget.blocks[blockId] %}
          <li class="logo-item" data-block-id="{{ blockId }}">
            {% if block.settings.link.href != blank %}
              <a
                href="{{ block.settings.link.href }}"
                class="logo-link"
                {% if block.settings.link.target == '_blank' %}
                  target="_blank" rel="noopener"
                {% endif %}
              >
                {% if block.settings.image != blank %}
                  {{ block.settings.image | image: 'small', 'logo-image', block.settings.name }}
                {% else %}
                  {% placeholder_image 'landscape', { "class": "logo-image" } %}
                {% endif %}
              </a>
            {% else %}
              {% if block.settings.image != blank %}
                {{ block.settings.image | image: 'small', 'logo-image', block.settings.name }}
              {% else %}
                {% placeholder_image 'landscape', { "class": "logo-image", "alt": "Logo" } %}
              {% endif %}
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</section>
